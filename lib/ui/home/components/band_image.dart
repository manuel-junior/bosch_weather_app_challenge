import 'package:flutter/widgets.dart';

class BandImage extends StatelessWidget {
  const BandImage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/band.jpg');
  }
}
